{
  "name": "creative-kg-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix website_FE run dev",
    "build": "npm --prefix website_FE run build",
    "preview": "npm --prefix website_FE run preview",
    "lint": "npm --prefix website_FE run lint",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:prod": "docker compose -f docker-compose.prod.yml up -d --build"
  }
}
